{{!-- 

<main>
    {{!-- foumulario --}}
    <div class="container">
        <div class="row">
            <div class="col-6 offset-3">
                <form action="/cadastrar" method="post" enctype="multipart/form-data">
                
                    <input type="text" name="nome" class="form-control" placeholder="Nome">
                    <input type="number" name = "idade" class="form-control"  placeholder="Idade">
                    <input type="email" name = "email" class="form-control" placeholder="Email">
                    <input type="paswd" name = "senha" class="form-control" placeholder="Senha">
                     <input type="file" name = "imagem" class="form-control" >
                    <input type="submit" class="btn btn-primary" value="Cadastrar"> 

                </form>
            </div>        
        </div>       
    </div>
    {{!-- Listagem de Clientes --}}

<div class="conteiner">
    <div class="row">
    {{#each clientes}}
        <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <div class="card">
                <img src="../imagens/{{imagem}}" class="card-img-top" alt="..."> 
                <div class="textos">     
                    <p>Nome: {{nome}}</p>
                    <p>Idade: {{idade}}</p>
                    <p>Email: {{email}}</p>
                    <p>Senha: {{senha}}</p>
                </div> 
                    <a href="/formularioEditar/{{codigo}}" class="btn btn-warning">Alterar</a>
                    <a href="/remover/{{id}}&{{imagem}}" class="btn btn-danger">Remover</a>
             </div>
        </div>
        {{/each}}
    </div>
</div>
</main> --}}

<form action="/submit" method="POST" enctype="multipart/form-data" class="form-container">
  <!-- Campo ID (opcional, geralmente oculto) -->
  <input type="hidden" name="id" value="{{id}}">

  <!-- Campo Nome -->
  <div class="form-group">
    <label for="nome">Nome:</label>
    <input type="text" id="nome" name="nome" value="{{nome}}" placeholder="Digite o nome" required>
  </div>

  <!-- Campo Idade -->
  <div class="form-group">
    <label for="idade">Idade:</label>
    <input type="number" id="idade" name="idade" value="{{idade}}" placeholder="Digite a idade" required>
  </div>

  <!-- Campo Email -->
  <div class="form-group">
    <label for="email">Email:</label>
    <input type="email" id="email" name="email" value="{{email}}" placeholder="Digite o email" required>
  </div>

  <!-- Campo Senha -->
  <div class="form-group">
    <label for="senha">Senha:</label>
    <input type="password" id="senha" name="senha" placeholder="Digite a senha" required>
  </div>

  <!-- Campo Imagem -->
  <div class="form-group">
    <label for="imagem">Imagem:</label>
    <input type="file" id="imagem" name="imagem" accept="image/*">
  </div>

  <!-- Botões -->
  <div class="form-actions">
    <button type="submit">Enviar</button>
    <button type="reset">Limpar</button>
  </div>
</form>
